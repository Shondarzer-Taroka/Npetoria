import React from 'react';
import FilterSidebar from '../FilterSidebar/FilterSidebar';

const Pets = () => {
    return (
        <div>
            <FilterSidebar/>
        </div>
    );
};

export default Pets;